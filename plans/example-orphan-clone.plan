# zhaxx repair plan — generated by zhaxx-scan
# REVIEW EVERY LINE against the report before use.
# Every op re-verifies its expect= precondition atomically before any write.
# break the errlog_last error-log chain
zap_set obj=1 key=errlog_last expect=910 value=0
# free stale errlog_last head
free obj=910 type="persistent error log"
# free per-objset error log for objset 673
free obj=912 type="persistent error log"
# zero the stale scan cursor (a fresh scrub/resilver restarts from DTLs after import)
zap_zero obj=1 key=scan count=24
# drop unreadable (EIO) clone reference from <root>/$ORIGIN dd_clones
zap_remove obj=59 key=2a1 expect=673
# remove orphan 'orphan-9f3c1d' from namespace
zap_remove obj=34 key=orphan-9f3c1d expect=670
# remove orphan from origin's next_clones ZAP
zap_remove obj=58 key=2a1 expect=673
# decrement origin snapshot child count
bonus_set obj=48 field=ds_num_children expect=6 value=5
# free orphan structure (DSL directory) of <root>/orphan-9f3c1d@head
free obj=670 type="DSL directory"
# free orphan structure (DSL props) of <root>/orphan-9f3c1d@head
free obj=671 type="DSL props"
# free orphan structure (DSL directory child map) of <root>/orphan-9f3c1d@head
free obj=672 type="DSL directory child map"
